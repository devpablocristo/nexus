apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus.tower.serviceName" . }}
  labels:
    {{- include "nexus.tower.labels" . | nindent 4 }}
spec:
  type: {{ .Values.tower.service.type }}
  ports:
    - port: {{ .Values.tower.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus.tower.selectorLabels" . | nindent 4 }}
