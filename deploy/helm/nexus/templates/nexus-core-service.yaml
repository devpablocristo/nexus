apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexus.core.serviceName" . }}
  labels:
    {{- include "nexus.core.labels" . | nindent 4 }}
spec:
  type: {{ .Values.core.service.type }}
  ports:
    - port: {{ .Values.core.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexus.core.selectorLabels" . | nindent 4 }}
