root = "."
tmp_dir = "tmp"

[build]
  cmd = "CGO_ENABLED=0 go build -o ./tmp/nexus-core ./cmd/api"
  bin = "./tmp/nexus-core"
  full_bin = "./tmp/nexus-core"
  include_ext = ["go", "sql"]
  exclude_dir = ["tmp", "vendor", "docs", "monitoring"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
