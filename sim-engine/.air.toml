root = "."
tmp_dir = "tmp"

[build]
  cmd = "CGO_ENABLED=0 go build -o ./tmp/sim-engine ./cmd/sim-engine"
  bin = "./tmp/sim-engine"
  full_bin = "./tmp/sim-engine"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
