name: V3 CI

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/v3-ci.yml"
      - "v3/**"
  pull_request:
    paths:
      - ".github/workflows/v3-ci.yml"
      - "v3/**"

concurrency:
  group: v3-ci-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  nexus:
    name: nexus tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout nexus
        uses: actions/checkout@v4
        with:
          path: nexus

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: nexus/v3/nexus/go.mod
          cache-dependency-path: nexus/v3/nexus/go.sum

      - name: Run go test
        working-directory: nexus/v3/nexus
        run: go test ./...

      - name: Run go vet
        working-directory: nexus/v3/nexus
        run: go vet ./...

      - name: Check migration versions
        working-directory: nexus/v3
        run: bash scripts/quality/check-migrations.sh

  console:
    name: console checks
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout nexus
        uses: actions/checkout@v4
        with:
          path: nexus

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version-file: nexus/v3/console/.nvmrc
          cache: npm
          cache-dependency-path: nexus/v3/console/package-lock.json

      - name: Install deps
        working-directory: nexus/v3/console
        run: npm ci

      - name: Typecheck console
        working-directory: nexus/v3/console
        run: npm run typecheck

      - name: Build console
        working-directory: nexus/v3/console
        run: npm run build

  smoke:
    name: smoke
    runs-on: ubuntu-latest
    needs:
      - nexus
      - console
    timeout-minutes: 25
    steps:
      - name: Checkout nexus
        uses: actions/checkout@v4
        with:
          path: nexus

      - name: Prepare env
        working-directory: nexus/v3
        run: test -f .env || cp .env.example .env

      - name: Validate compose model
        working-directory: nexus/v3
        run: docker compose config --services

      - name: Start nexus stack
        working-directory: nexus/v3
        run: docker compose up -d --build governance-postgres nexus

      - name: Run smoke suite
        working-directory: nexus/v3
        run: make smoke

      - name: Capture compose logs
        if: always()
        working-directory: nexus/v3
        run: docker compose logs --no-color > smoke-docker-compose.log

      - name: Upload smoke logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v3-smoke-docker-compose-logs
          path: nexus/v3/smoke-docker-compose.log

      - name: Tear down stack
        if: always()
        working-directory: nexus/v3
        run: docker compose down -v --remove-orphans
