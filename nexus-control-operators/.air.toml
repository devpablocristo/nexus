root = "."
tmp_dir = "tmp"

[build]
  cmd = "CGO_ENABLED=0 go build -o ./tmp/ops-workers ./cmd/ops-workers"
  bin = "./tmp/ops-workers"
  full_bin = "./tmp/ops-workers"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
