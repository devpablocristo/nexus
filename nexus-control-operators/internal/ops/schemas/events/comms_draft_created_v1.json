{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://nexus.example/schemas/ops/events/comms_draft_created_v1.json",
  "title": "comms.draft_created payload v1",
  "type": "object",
  "required": ["draft_id", "incident_id", "channel", "audience", "content"],
  "properties": {
    "draft_id": { "type": "string", "format": "uuid" },
    "incident_id": { "type": "string", "format": "uuid" },
    "channel": { "type": "string", "enum": ["internal", "external", "status_page"] },
    "audience": { "type": "string", "minLength": 1 },
    "content": { "type": "string", "minLength": 1 }
  },
  "additionalProperties": true
}
